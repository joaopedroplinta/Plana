Use the web-agent to scaffold the following frontend feature: $ARGUMENTS

The agent must create:
1. Page component in the correct app/ route group
2. TypeScript types in src/types/
3. Service function in src/services/
4. Any reusable components in src/components/shared/
5. Loading and error states
6. Proper route protection via middleware or layout

Run npm run build at the end to verify TypeScript compiles without errors.
